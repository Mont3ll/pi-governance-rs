# pi-governance-mcp changelog

This crate is part of the PI Governance workspace.

## v1.1.0

Added `pi.memory_graph`, `pi.memory_quality`, `pi.relationship_quality`, `pi.recall_effectiveness`, `pi.store_quality`, `pi.simulate_patch`, `pi.procedure_candidates`, `pi.failure_analysis`, and `pi.recall_feedback`. Initialize and doctor responses now expose the resolved store path and active namespace identity.

For full project release notes, see:
https://github.com/Mont3ll/pi-governance-rs/releases
