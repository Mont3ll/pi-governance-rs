# GitHub Release Plan: v1.0.0

Repository: https://github.com/Mont3ll/pi-governance-rs
License: MIT OR Apache-2.0

## Release title

PI Governance v1.0.0

## Release summary

Stable local-first governed memory CLI and MCP stdio server for AI agents. `pi-governance-rs` can be used standalone or alongside `pi-persistent-intelligence` through the shared PI memory contract.

## Assets to upload

- Source zip generated by GitHub.
- Source tarball generated by GitHub.
- Relative patch: `pi-governance-rs-v1.0.0-packaging-relative.patch`.
- Optional local archive: `pi-governance-rs-v1.0.0-packaging.zip`.
- Checksums for uploaded artifacts.

## Optional compiled binaries

Only build these when toolchains are configured:

- `pi-governance-rs-v1.0.0-x86_64-unknown-linux-gnu.tar.gz`
- `pi-governance-rs-v1.0.0-x86_64-apple-darwin.tar.gz`
- `pi-governance-rs-v1.0.0-aarch64-apple-darwin.tar.gz`
- `pi-governance-rs-v1.0.0-x86_64-pc-windows-msvc.zip`

## Release notes

- Local-first governed memory CLI and MCP stdio server.
- Stable CLI and MCP tool surface for v1.0.0.
- No hosted service by default.
- Compatible with `pi-persistent-intelligence` via import/export and shared schemas.

## OpenCode exception wording

OpenCode install/doctor validation passed. A previous live client run was incomplete due environmental/client-run limitations and did not demonstrate a PI Governance MCP server failure.

## Install examples

```bash
cargo install --git https://github.com/Mont3ll/pi-governance-rs --tag v1.0.2 pi-governance-rs
pi --version
```

After crates.io publishing is explicitly approved:

```bash
cargo install pi-governance-rs
```

## MCP setup examples

```bash
pi mcp-config opencode --command /path/to/pi --store /path/to/.pi --namespace default
pi mcp-config codex --command /path/to/pi --store /path/to/.pi --namespace default
pi mcp-config pi-agent --command /path/to/pi --store /path/to/.pi --namespace default
```
